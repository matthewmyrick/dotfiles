-- Disable LazyVim default plugins that we want to customize
return {
  -- Note: We don't disable bufferline here anymore since it conflicts with our custom one
  -- Instead we override LazyVim's configuration in ui.lua
}