# new features 

## help menu 

when we press ? we should be able to see all the commands and description in a large pane that is transparent

## download feature

when we press d it will download the document (then it will ask you for a location in a floating pane and default to the ~/Downloads folder)

## delete feature 

when we press D it will delete the document but first asking if we are sure. 

## edit feature

if i am hovering over a document or have it opened and i press e it will put me into an edit mode where i can edit the codument like i am in neovim (so just a normal buffer)

This buffer needs to be editable then when i write to it <leader>ww or :w it will push it to the bucket. Can we make sure we add a little notification or soemthing that we saved it? 

## copying files

if i my cursor in on the file i can press y 

it will copy that file then when i press p under a folder it will copy that file to another folder in the s3...

We cna do this across buckets as well if we want 

## moving files

if i press m it will highlight that file in a subtle color then i can press 

we can do this across buckets as well if we want

## renaming the file/folder

if i press r it will prompt us to put in the new name or rename it then when i press enter it will rename it

there will be a prompt as well that is are you sure yes or no in a pane


